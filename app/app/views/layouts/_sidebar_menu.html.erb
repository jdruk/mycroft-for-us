<%= link_to root_path, class: "#{is_current_page?(root_path)} item" do  %>
	<%= image_tag  "mycroft-logo.png", :style => "max-width: 108px;"%>
<% end %>

<% if current_user.admin? %>

  <%= link_to dashboard_path, class: "#{is_current_page?(dashboard_path)} item" do  %> 
  <i class="chart area icon"></i>Dashboard
	<% end %> 

  <%= link_to users_path, class: "#{is_current_page?(users_path)} item" do  %> 
  <i class="user circle icon"></i>Usuários
	<% end %> 
  
  <%= link_to clients_path, class: "#{is_current_page?(clients_path)} item" do  %>
	<i class="users circle icon"></i>Clientes
	<% end %>

	<%= link_to concentrators_path, class: "#{is_current_page?(concentrators_path)} item" do  %>
	<i class="cogs icon"></i>Concentradores
	<% end %> 

	<%= link_to plans_path, class: "#{is_current_page?(plans_path)} item" do  %>
	<i class="download icon"></i>Planos de dados
	<% end %>

	<%= link_to plans_path, class: "#{is_current_page?(root_path)} item" do  %>
	<i class="clipboard list icon"></i>Relatórios
	<% end %>
<% end %>

<%= link_to financial_dashboard_path, class: "#{is_current_page?(financial_dashboard_path)} item" do  %>
<i class="credit card icon" ></i>Financeiro
<% end %>   
